spring:
  config:
    activate:
      on-profile: prodDB # prodDB 프로파일에서만 활성화
  cloud:
    config:
      uri: http://172.31.10.0:8888
      name: game-service
      profile: prodDB
      enabled: true
